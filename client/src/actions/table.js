import axios form 'axios'
import { setAlert } from "./alert";
import {
  GET_TABLES,
  TABLE_ERROR
} from './types';

// Get tables
export const getTables = () => async dispatch => {
  try {

  } catch (error) {
    
  }
};
